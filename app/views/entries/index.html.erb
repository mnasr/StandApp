<% content_for :title, @title %>
<div class="page-header">
  <h1><%= @title %></h1>
</div>

<table class="table table-striped">
  <tr>
    <th>Description</th>
    <th>Category</th>
    <th>User</th>
    <th>Ticket</th>
    <th></th>
  </tr>

<% @entries.each do |entry| %>
  <tr>
    <td><%= link_to entry.description, edit_entry_path(entry) %></td>
    <td><%= entry.category %></td>
    <td><%= entry.user.email %></td>
    <td><%= entry.ticket_id %></td>
    <td><%= button_to('Destroy', entry, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-danger') %></i> </td>
  </tr>
<% end %>
</table>

<br />

<%= button_to('New Entry', new_entry_path, :method => :get, :class => 'btn btn-primary')%>
   