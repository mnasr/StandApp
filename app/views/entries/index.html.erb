<% content_for :title, @title %>

<div class="page-header">
  <h1><%= @title %></h1>
</div>

<table class="table table-striped">
  <tr>
    <th>Description</th>
    <th>Category</th>
    <th>User</th>
    <th>Ticket</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @entries.each do |entry| %>
  <tr>
    <td><%= link_to entry.description, entry %></td>
    <td><%= entry.category %></td>
    <td><%= entry.user.email %></td>
    <td><%= entry.ticket_id %></td>
    <td><%= button_to('Edit', edit_entry_path(entry), :class => 'btn btn-primary') %></td>
    <td><%= button_to('Destroy', entry, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-danger') %></i> </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Entry', new_entry_path %>
   