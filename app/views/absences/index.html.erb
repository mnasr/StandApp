<% content_for :title, @title %>

<div class="page-header">
	<div class="row-fluid">
		<div class="span10"><h1><%= @title %></h1></div>
  	<div class="span2" id="entry_align">
			<%= link_to("Today's absences", today_absences_path, :class => 'btn btn-primary') %>
		</div>
	</div>
</div>

<%= render :partial => "absences/all_absences" %> 
  