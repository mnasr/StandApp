<% content_for :title, "Absences" %>

<div class="page-header">
<h1>Absences</h1>
</div>
   <td><%= link_to('Absences For Today', today_absences_path, :class => "btn") %></td>
   <p>                                                        
</p>
<table class="table table-striped">

  <tr>
    <th>User</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @absences.each do |absence| %>
  <tr>
    <td><%= absence.user_id %></td>
    <td><%= absence.description %></td>
    <td><%= link_to('Edit', edit_absence_path(absence), :class => "btn btn-primary") %></td>
    <td><%= link_to('Destroy', absence, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-danger') %></i> </td>
  </tr>
<% end %>
</table>
<br />
 
 