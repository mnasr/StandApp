<% content_for :title, "Listing users" %>

 <div class="page-header">
<h1>Listing users</h1>
</div>

<table class="table table-striped">
  <tr>
    <th>Fullname</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.fullname %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to('Absences', user_absences_path(user)) %></td>
    <td><%= link_to('Edit', edit_user_path(user), :class => 'btn btn-primary') %></td>
    <td><%= link_to('Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-danger') %></i> </td>

  </tr>
<% end %>
</table>
<br />

<%= link_to 'New User', new_user_path %>